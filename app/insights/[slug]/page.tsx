import Link from "next/link";
import { ArrowLeft, ArrowUpRight } from "lucide-react";
import { articles } from "../articles";
import { Kicker } from "../../../components/Section";

export function generateStaticParams() { return articles.map(article => ({ slug: article.slug })); }
export default async function Article({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = articles.find(item => item.slug === slug); if (!article) return <main className="article-detail"><Kicker>JOURNAL</Kicker><h1>That note<br /><em>does not exist.</em></h1><Link className="text-link" href="/insights">Back to insights <ArrowUpRight size={16} /></Link></main>; return <main className="article-detail"><Link className="back-link" href="/insights"><ArrowLeft size={16} /> Back to insights</Link><Kicker>{article.category} · {article.date}</Kicker><h1>{article.title}</h1><p className="article-dek">{article.excerpt}</p><div className="article-body"><p>Building something meaningful rarely begins with certainty. It begins with paying attention: to a repeated problem, an unusual behavior, or a question that will not leave you alone.</p><p>The useful work is in the staying. Listening longer. Testing earlier. Choosing the version of the idea that helps a real person rather than the version that sounds best in a room.</p><h2>Make the question sharper.</h2><p>At Prabidhi Uthaan, we believe strong companies are built through a sequence of honest decisions. The first one is deciding what is worth building at all.</p></div><Link className="button" href="/pitch">Have an idea? Pitch it <ArrowUpRight size={18} /></Link></main>; }
